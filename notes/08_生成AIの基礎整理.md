# 生成AIの基礎整理

## 位置づけ

このノートは試験の主戦場ではありません。  
ただし、`RAG と fine-tuning の切り分け`、`推論パラメータの意味`、`プロンプト改善の優先順位` を安定して判断する土台として有効です。

要点:

- 生成 AI は `文脈に条件づけられた次トークン予測` を繰り返して出力する
- 実務では `言い回し` より `何を渡すか` と `どう評価するか` が重要
- `temperature` は多様性の調整であり、正しさの保証ではない
- 最新知識や社内知識は `RAG`、振る舞いや形式の固定は `fine-tuning`

## 理解の土台

### 生成AIをどう見るか

- モデルは `知っていることを完璧に再生する装置` ではない
- 与えられた文脈から、もっともらしい次トークン列を作る装置
- だから `何を入れるか` と `どう制約するか` が品質を大きく左右する

要点:

- `賢いモデルなら全部解決する` と考えると、RAG、structured outputs、評価の必要性を見失いやすい
- 試験でも、モデル能力そのものより `周辺設計で安定化する発想` が重要

## 1. まず押さえる仕組み

### トークン

- モデルは文章をそのまま理解するのではなく、まず `token` の列として扱う
- トークン数はそのまま `コスト`、`レイテンシ`、`context window` 圧迫に効く
- 試験では `長い入力をどう減らすか` が運用最適化の基本論点

### 埋め込み

- テキストや画像を数値ベクトルへ変換して近さを比較しやすくする
- `Knowledge Bases`、`ベクトル検索`、`類似検索` の前提
- 埋め込みは `検索用の表現` であり、生成そのものとは役割が違う

### 次トークン予測

- LLM は、直前までの文脈から `次に出すトークン` を確率的に選ぶ
- 要約、分類、翻訳、コード生成も、この枠組みの上で成立する
- 自然な文章を返せても、内容の正確さまでは自動で保証されない

### 文脈

- 出力は `system 指示`、`会話履歴`、`検索結果`、`ツール結果`、`出力形式指定` に強く依存する
- 同じモデルでも、渡す文脈が変われば品質は大きく変わる
- したがって、実務では `モデル性能` だけでなく `context 設計` が重要

### なぜ文脈設計が重要か

- モデルは、いま見えている情報から答える
- 必要情報がないと、空白を埋めるようにもっともらしい答えを作りやすい
- 情報が多すぎても、重要部分が埋もれて逆に不安定になる

補足:

- つまり `足りない` と `多すぎる` の両方が品質低下につながる
- ここが、`全部入れる` より `必要なものだけ検索して渡す` が有利な理由

## 2. プロンプトはどう考えるか

### いま重要なこと

- 目的を明示する
- 制約を明示する
- 出力形式を固定する
- 不明な場合の扱いを指定する
- 必要な資料だけを渡す

要点:

- `うまい言い回し` を探すより、`成功条件` を明確にする方が効く
- 自由文より `structured outputs` の方が後続処理に載せやすい
- 外部データや API が必要なら、プロンプトだけで頑張らず `RAG` や `tool use` を使う

### プロンプト改善より先に見ること

1. 成功条件が定義されているか
2. 必要な文脈を渡せているか
3. 出力形式を固定しているか
4. 評価方法があるか
5. それでも不足するなら prompt やモデルを見直す

### なぜ prompt だけでは限界があるか

- prompt は `指示の明確化` には効く
- しかし `最新知識の追加`、`外部処理の実行`、`形式保証` は他の仕組みの方が強い
- そのため実務では、prompt を磨く前に `RAG`、`tool use`、`structured outputs` を検討する

## 3. 推論パラメータの見方

### temperature

- 低いほど上位候補に寄りやすく、出力が安定しやすい
- 高いほど候補が広がり、多様性が増えやすい
- `hallucination を直接なくす機能` ではない

### top-p

- 確率上位の候補集合から選ぶ範囲を調整する
- temperature と同様に `ランダムさの制御` に関わる
- 正確性の問題は、`文脈不足`、`検索不足`、`モデル選択不一致` の方を先に疑う

### max tokens

- 出力長の上限
- 冗長な回答を防ぎ、コストとレイテンシを抑える基本設定

試験向けの整理:

- `安定した短い業務応答` -> temperature を低めにし、出力形式を固定する
- `創造的な案出し` -> 多様性パラメータの調整余地がある
- `事実性が足りない` -> まず RAG、参照データ、Guardrails、評価を見直す

## 4. よくある誤解

### 低い temperature なら正しくなる

- 誤りです
- 低温では答えが安定しやすいだけで、安定して間違うこともある

### 長い context を全部入れれば解決する

- 誤りです
- ノイズが増え、重要情報が埋もれ、コストも上がる
- 必要な情報だけを検索して渡す方がよい

### 最新知識が欲しいなら fine-tuning

- 多くは誤りです
- 最新文書、社内文書、頻繁更新データは `RAG` や外部データ参照が第一候補

### 出力形式のブレは prompt を長くすれば解決する

- 限界があります
- `structured outputs`、`schema 制約`、`tool use` の方が実装では強い

## 5. 試験との接続

### Domain 1

- `トークン`、`埋め込み`、`チャンク分割`、`ベクトル検索` を一つの流れで理解しやすくなる
- `最新知識は RAG、振る舞い変更は fine-tuning` を切り分けやすくなる

### Domain 2

- `prompt だけで解くか`、`tool use を使うか`、`Agents にするか` の判断が安定する
- `structured outputs` を優先する理由が明確になる

### Domain 4

- `token efficiency`、`prompt caching`、`max tokens` の意味がつながる
- レイテンシとコストの改善で `まず入力を減らす` 発想を持てる

### Domain 5

- 品質改善では `思いつきの prompt 修正` より `evaluation` が先だと整理できる
- 変更の比較には `同じ入力集合` と `同じ採点基準` が必要

## Active Recall

- 生成 AI の出力は何に最も強く依存するか
- `temperature` は何を調整し、何を保証しないか
- 最新知識を答えさせたいとき、第一候補は何か
- 出力形式のブレを減らしたいとき、自由文の工夫だけで十分か
- prompt 改善の前に定義すべきものは何か

### 答えと解説

1. 文脈です。system 指示、履歴、検索結果、ツール結果、出力形式指定が効きます。
2. 出力の多様性やばらつきです。事実性や正しさは保証しません。
3. 多くのケースでは `RAG` や外部データ参照です。更新知識を重みに焼き込むより自然です。
4. 十分ではありません。`structured outputs` や schema 制約の方が安定します。
5. 成功条件と評価方法です。ここが曖昧だと改善比較ができません。

## 公式情報

- [Technologies and concepts that might appear on the exam](https://docs.aws.amazon.com/aws-certification/latest/examguides/ai-professional-01-technologies-concepts.html)
- [What is Amazon Bedrock?](https://docs.aws.amazon.com/bedrock/latest/userguide/what-is-bedrock.html)
- [What is prompt engineering?](https://docs.aws.amazon.com/bedrock/latest/userguide/what-is-prompt-engineering.html)
- [Prompt engineering concepts](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-engineering-guidelines.html)
- [Inference request parameters and response fields for foundation models](https://docs.aws.amazon.com/bedrock/latest/userguide/model-parameters.html)
- [Optimize costs using prompt caching in Amazon Bedrock](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html)
